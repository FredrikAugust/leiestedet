<h1>Annonser</h1>
<hr>

<% @listings.each do |listing| %>
  <h2><%= link_to listing.title, listing_path(listing) %></h2>
  <small>Annonse laget av: <%= listing.user.first_name + ' ' + listing.user.last_name +
                               ', ' + listing.location %></small>
  <p><%= listing.description %></p>
<% end %>
