<!DOCTYPE html>
<html lang='no'>
  <head>
    <title>Leiestedet</title>
    <%= csrf_meta_tags %>

    <meta charset='utf-8'>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <div class="nav-wrapper container">
        <a class="brand-logo" href="<%= root_path %>">Leiestedet</a>
        <ul class="nav-mobile right hide-on-med-and-down">
          <li><a href="<%= listing_index_path %>">Annonser</a></li>
          <li><a href="<%= root_path %>">Om oss</a></li>
          <% if user_signed_in? %>
            <li><%= link_to 'Ny annonse', new_listing_path %></li>
            <li><%= link_to 'Logg ut', destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><a href="<%= new_user_session_path %>">Logg inn</a></li>
            <li><a href="<%= new_user_registration_path %>">Registrer deg</a></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>

    <script>
      <% flash.each do |key, value| %>
        Materialize.toast("<%= value %>", 3000);
      <% end %>
    </script>
  </body>
</html>
